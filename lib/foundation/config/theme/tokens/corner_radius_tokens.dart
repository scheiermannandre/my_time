/// A collection of predefined corner radius values for consistent styling.
class CornerRadiusTokens {
  /// A very small corner radius value (4.0).
  static const verySmall = 4.0;

  /// A slighty small corner radius value (6.0).
  static const slightySmall = 6.0;

  /// A small corner radius value (8.0).
  static const small = 8.0;

  /// A medium corner radius value (16.0).
  static const medium = 16.0;

  /// A large corner radius value (24.0).
  static const large = 24.0;

  /// A very large corner radius value (32.0).
  static const veryLarge = 32.0;
}
