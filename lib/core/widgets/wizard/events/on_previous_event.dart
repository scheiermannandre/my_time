/// An event class representing the "onPrevious" event in a wizard.
///
/// The `OnPreviousEvent` class is used to signal that the user has moved
/// to the previous step in a wizard.
class OnPreviousEvent {}
