import 'package:flutter/material.dart';

class TimeEntryForm extends StatelessWidget {
  const TimeEntryForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Under Construction!"));
  }
}
